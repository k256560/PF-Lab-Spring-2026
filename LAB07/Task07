#include <stdio.h>
int main() {
    int a[10]={2,3,5,3,5,9,1,2,8,1};
   int i;
   int seen[101]={0};
    for(i = 0; i < 10; i++) {
        if(seen[a[i]] == 1) {
            int temp = a[i]; 
            a[i] = -1;
        } else {
            seen[a[i]]= 1;
        }
    }
    for(i = 0; i < 10; i++) 
    printf("%d ", a[i]);
    return 0;
}
