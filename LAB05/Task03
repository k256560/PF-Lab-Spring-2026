#include <stdio.h>
int main() {
    int vehicleType, parkingType;
    int hours;
    int rate = 0;
    double totalFare;
    printf("-------Vehicle Type-----------\n");
    printf("1. Car\n");
    printf("2. Bike\n");
    printf("3. Bus\n");
    printf("Enter your choice: ");
    scanf("%d", &vehicleType);

    printf("Enter number of hours parked: ");
    scanf("%d", &hours);
    
    switch(vehicleType) {
        case 1: 
        printf("----Parking Type----\n");
        printf("1. Regular Parking \n");
        printf("2. VIP Parking \n");
        printf("Enter your choice (1-2): ");
            switch(parkingType) {
            case 1:
            rate = 50;
            break;
            
			case 2:
            rate = 100;
            break;
            
			default:
            printf("Invalid Parking Type!\n");
            return 0;
            }
            break;

        case 2: 
        printf("----Parking Type----\n");
        printf("1. Regular \n");
        printf("2. Premium \n");
        printf("Enter your choice: ");
        scanf("%d", &parkingType);
            switch(parkingType) {
            case 1:
            rate = 20;
            break;
            
            case 2:
            rate = 40;
            break;
            
            default:
            printf("Invalid Parking Type !\n");
            return 0;
            }
            break;

        case 3:
        rate = 200;
        break;

        default:
        printf("Invalid Vehicle Type!\n");
        return 0;
    }

    totalFare = rate * hours;
    printf("Total Parking Fare: Rs. %2lf\n", totalFare);

    return 0;
}
