#include<stdio.h>
int main()
{
	float CGPA;
	double familyincome;
	printf("ENTER CGPA: ");
	scanf("%f",&CGPA);
	printf("ENTER familyincome: ");
	scanf("%lf",&familyincome);
     if(CGPA>=3.5)
     {
     	if(familyincome<50000)
     	{printf("100 percent scholarship");
		 }
		 else{printf("50 percent scholarship");
		 }
	 }
	 else{
	 	if(CGPA>=3.0&&familyincome<40000){
	 		printf("25 percent scholarship");
		 }
		 else{
		 	printf("No scholarship");
		 }
	 }
	 return 0;
}
