#include <stdio.h> 
int main() { 
    int n; 
    int total = 0; 
    printf("Enter number of days: "); 
    scanf("%d", &n); 
    for(int i = 1; i <= n; i++) { 
        total=total+i; 
    } 
    printf("Total distance covered in %d days: %d km\n", n, total); 
    return 0; 
} 
